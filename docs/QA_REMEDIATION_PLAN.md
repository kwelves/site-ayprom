# План устранения QA-дефектов

## Паспорт документа

| Поле | Значение |
|---|---|
| Статус | Активный источник истины; фазы 0–4, внеочередные 4A и 4B, фаза 6, фаза 7A и фаза 8 завершены. QA-001…QA-005, QA-007…QA-009 и QA-011…QA-014 закрыты; QA-006 и QA-010 остаются открытыми |
| Дата baseline | 2026-08-24 |
| Baseline Git | `a201d1e` (`main`) |
| Область | Публичный каталог, административная панель, Supabase, Storage, сборка и эксплуатационные проверки |
| Правило обновления | После каждого checkpoint обновлять статусы, решения, результаты проверок и changelog этого документа |

Этот документ фиксирует согласованный порядок устранения QA-находок. Реестр объединяет подтверждённые кодом дефекты и риски/гипотезы, которым ещё нужна browser-проверка; запись в реестре сама по себе не доказывает дефект и не означает, что он уже исправлен. На baseline в репозитории не было конфигурации Playwright и самостоятельного E2E-набора; фаза 1 добавила и независимо проверила воспроизводимый browser harness.

### Иерархия источников

- `PROJECT_BRIEF.md` задаёт три фиксированных продуктовых ограничения и живую продуктовую направленность.
- Этот документ является рабочим источником истины для QA-фаз 0–8, acceptance evidence и статусов закрытия.
- `docs/ROADMAP.md` сохраняется как исторический контекст раннего аудита и прежних решений. Его фазы не соответствуют QA-фазам этого документа; конфликтующие, устаревшие или незавершённые инструкции из `ROADMAP` не исполняются без повторной проверки актуального состояния и отдельного разрешения пользователя.

## Цель и границы

Цель — устранить подтверждённые дефекты, проверить отмеченные риски и снизить вероятность регрессий без редизайна, смены продуктовой модели или незаявленного расширения функциональности.

В рамках плана:

- укрепить целостность продуктовых мутаций и контрактов сортировки;
- заменить уязвимую архитектуру загрузки нескольких фотографий на staged upload;
- выполнять distributed rate-limit reservation до дорогой проверки пароля;
- обеспечить быстрый первый движущийся кадр hero через лёгкую стартовую версию и бесшовное повышение до качественной версии без ухудшения master;
- связать сохранённые SEO-поля с публичными metadata и закрыть глобальный 404;
- исправить доступность, узкие экраны и нестабильные области нажатия;
- сделать проверки и сборку воспроизводимыми.

Вне рамок:

- новый визуальный стиль, новая информационная архитектура или смена навигационной модели;
- цены, корзина, оплата, checkout, статусы наличия и AI-поиск;
- автоматическое изменение production-БД, Supabase Storage, DNS, Cloudflare или deployment;
- удаление или замена качественных media master ради произвольного ограничения размера.

## Фиксированные продуктовые ограничения

`PROJECT_BRIEF.md` прямо называет фиксированными только следующие ограничения:

1. Сайт остаётся каталогом, а не интернет-магазином: нет корзины, онлайн-оплаты и checkout.
2. В первой версии цены не показываются.
3. Каталожные данные и административный доступ должны оставаться защищёнными.

Они не меняются этим remediation-планом. Остальные положения brief являются текущей направленностью и могут уточняться по его собственным правилам после обсуждения tradeoffs.

## Текущие scope-preservation assumptions

Ниже зафиксирован текущий контракт реализации и README, который сохраняется в рамках QA-remediation, но не объявляется вечным immutable-ограничением `PROJECT_BRIEF.md`:

1. Supabase остаётся источником истины production-каталога; production-страницы не используют hardcoded mock catalog fallback.
2. Публичное чтение использует publishable key и защищено RLS; secret/service-role key доступен только серверному коду.
3. Изменения схемы фиксируются версионированными SQL-миграциями, ревьюятся и локально тестируются до remote application.
4. Каталог рассчитан примерно на 2 000 товаров; backend pagination/search сохраняются. Лимит **24 карточки на текущую страницу** — действующий implementation/README contract, а не неизменяемый продуктовый предел.
5. Поиск остаётся основным способом сужения каталога и охватывает название, артикул, бренд, категорию и характеристики. AI-поиск не добавляется в рамках этого плана.
6. Сохраняются Next.js App Router, TypeScript, Tailwind CSS и переиспользуемые компоненты.
7. Качественный raster/video master хранится минимум в 2K, предпочтительно 4K. Для доставки создаются проверенные responsive derivatives; master не ухудшается только ради файлового бюджета.
8. SVG остаётся векторным: его нельзя растрировать или апскейлить; должны сохраняться корректный `viewBox`, paths и отсутствие встроенного низкокачественного raster.
9. Обязательные публичные страницы, административные разделы, состав карточки/страницы товара и заметный доступный WhatsApp badge сохраняются.
10. Статусы наличия, новый visual style, новая navigation model и другие неоговорённые продуктовые расширения не вводятся в рамках remediation. Это граница текущего scope, а не новое фиксированное требование brief.

## Severity и статусы

- `S0 Critical` — риск обхода security boundary, потери/повреждения данных или недоступности критического потока.
- `S1 High` — существенный production-дефект, ненадёжный ключевой сценарий или крупная регрессия производительности.
- `S2 Medium` — заметный функциональный, SEO, accessibility или responsive-дефект с обходным путём.
- `S3 Low` — качество кода/сборки и долговременная поддерживаемость без немедленного пользовательского ущерба.
- Статусы: `Открыт`, `В работе`, `Заблокирован`, `Готов к проверке`, `Закрыт`, `Принят риск`.

## Реестр QA-находок

| ID | Severity | Evidence / текущее поведение baseline | Целевое поведение | Области | Фаза | Статус |
|---|---|---|---|---|---:|---|
| QA-001 | S1 | В baseline нет `playwright.config.*` и выделенного E2E-набора; текущие unit/integration проверки не подтверждают реальное browser-поведение. | Воспроизводимый preflight и Playwright smoke/regression suite для критических public/admin flows, desktop/mobile и консольных ошибок. | test tooling, CI, docs | 1 | Закрыт |
| QA-002 | S0 | `createProduct` вставляет товар, затем связанные таблицы отдельными запросами с компенсирующим delete; `updateProduct` обновляет row и пересоздаёт связи отдельными операциями. Частичный сбой не является единой DB-транзакцией. | `create_product_with_relations` и `update_product_with_relations` выполняют связанные изменения атомарно, проверяют вход и конкуренцию; ошибка не оставляет частичное состояние. | admin actions, Postgres, migrations, pgTAP | 2 | Закрыт |
| QA-003 | S1 | Reorder RPC выполняют update по переданным идентификаторам, но большинство не доказывают полноту, уникальность и принадлежность переданного набора; image reorder action отдельно принимает `productSlug`, не передавая его RPC. | Строгие контракты: дубликаты, неизвестные/чужие ID и неполный недопустимый набор отвергаются; допустимый filtered/page reorder сохраняет оговорённые slots атомарно. | admin DnD, Postgres RPC, pgTAP | 2 | Закрыт |
| QA-004 | S1 | Create-mode передаёт все `photos` внутри одного Server Action `FormData`, до создания товара валидирует и обрабатывает их, затем сервер загружает их отдельными параллельными server-side Storage-вызовами в публичный `product-images`. Поток зависит от общего request payload/CPU/time budget. | Сначала создаётся draft, файлы независимо и напрямую грузятся по ограниченным signed tokens в private staging bucket. Finalize повторно валидирует originals; DB metadata изменяется атомарно внутри Postgres, а Storage+DB координируются идемпотентной state machine без обещания общей ACID-транзакции. Объект не публикуется до успешной revalidation/finalize; cleanup/reconcile безопасно устраняет незавершённые состояния. | ProductForm, Server Actions, Storage, lifecycle | 3 | Закрыт |
| QA-005 | S0 | `login` сначала получает credential и выполняет `verifyAdminPassword`/PBKDF2, а `registerLoginAttempt(passwordIsValid)` вызывается после проверки. Blocked-запрос всё равно расходует дорогой password-hash budget. | Атомарная reservation до PBKDF2 отклоняет blocked/overflow попытки; завершение reservation фиксирует success/failure; защита остаётся distributed и fail-closed. | admin auth, Postgres RPC, observability | 4 | Закрыт |
| QA-006 | S1 | Hero сразу рендерит единственную 2K `autoPlay`-версию с `preload="metadata"`: desktop 25 544 496 B, mobile 22 588 953 B. Boot overlay ждёт `loadeddata` (первый декодированный кадр), но не доказывает устойчивое воспроизведение; аварийный timeout может открыть hero без движущегося видео. | Двухступенчатый hero: лёгкая стартовая версия запускается под фиксированным boot overlay, overlay растворяется только после подтверждённого воспроизведения и отрисованного движущегося кадра. Затем качественная версия всегда догружается в фоне и бесшовно, синхронно заменяет стартовую; медленная сеть не оставляет пустой/чёрный/неподвижный hero и влияет только на длительность стартового качества. | Hero, boot sequence, media pipeline, performance | 5 | Открыт |
| QA-007 | S2 | Админка сохраняет `meta_title`/`meta_description`, но публичный `PRODUCT_SELECT` и `Product` mapping их не читают; публичная metadata не может использовать сохранённые значения. | Единый metadata helper использует валидные поля товара с безопасными fallback и canonical; существующие значения становятся видимыми в публичных metadata. | public queries, product routes, SEO tests | 6 | Закрыт |
| QA-008 | S2 | Есть сегментный `src/app/(site)/not-found.tsx`, но нет корневого/global not-found для запросов вне public route group; `globalNotFound` не включён. | Полная русскоязычная глобальная 404-страница корректно работает для неизвестного URL при нескольких root layouts и содержит собственные HTML/body зависимости. | Next config, app routing, SEO/E2E | 6 | Закрыт |
| QA-009 | S2 | В header часть disclosure-controls не связана через `aria-controls`; требуется полная проверка Escape/focus return. Login page использует внешний `div`, не `<main>`. `BackButton` без fallback всегда вызывает `router.back()`. | Семантика, accessible name/state/relationship, Escape, predictable focus, безопасный fallback back navigation и keyboard-visible focus подтверждены браузером. | header, admin login, navigation, a11y | 7A | Закрыт |
| QA-010 | S2 | Real browser evidence подтвердил P2-наблюдения: каталог визуально слишком плотный на 320/390, floating WhatsApp button перекрывает область карточки, а на странице встречается большой белый интервал при прокрутке. Исправление дизайна не подтверждено и отложено до фазы 7; анимированный/dragged target карусели также остаётся в системном reflow-аудите. | Browser-аудит сначала фиксирует воспроизводимые случаи; исправляются только подтверждённые overflow/overlap/click defects. На проверенных экранах нет непреднамеренного overflow, controls не перекрываются, touch-targets около 44 px, а подтверждённо нестабильная карусель получает hit-wrapper без изменения характера motion. | responsive UI, carousel, mobile admin/public | 7B | Открыт |
| QA-011 | S3 | `poweredByHeader` явно не выключен; font/build/dependency contracts и неиспользуемые файлы требуют инвентаризации. Сборка есть, но её детерминизм и чистый-install путь должны быть закреплены. | Локальный шрифт без runtime fetch, `poweredByHeader: false`, прямые зависимости и Node contract согласованы, dead code удалён только после доказательства, clean install/check/build воспроизводимы. | Next config, fonts, package, repo hygiene | 8 | Закрыт |
| QA-012 | S1 | Границы scale закрыты в фазе 2: значения измерены read-only по production (все заполненные лежали в 0.75–1.6, `product_images.scale` не заполнен ни разу), диапазон 0.1–5.0 закреплён в UI, на сервере и CHECK-констрейнтами на всех трёх колонках. Server-side проверка содержимого закрыта в фазе 3: обнаружено, что логотипы брендов и картинки категорий не проверялись вообще, а расширение бралось из имени файла; теперь тип и расширение выводятся из проверенного содержимого, SVG обезвреживается через isomorphic-dompurify с отказом (а не тихой вычисткой), а сами Storage buckets получили file_size_limit и allowed_mime_types. | Scale bounds закреплены UI + server + DB constraints; опасные/несовместимые файлы отклоняются по содержимому, а не только имени/MIME. | validation, DB constraints, uploads | 2–3 | Закрыт |
| QA-013 | S1 | Клиентские роли `anon` и `authenticated` имеют на таблицах каталога права, которых публичный сайт не использует и которые RLS не контролирует. Подтверждено чтением каталога прав: **на production** у обеих ролей на всех 11 таблицах каталога есть `SELECT, REFERENCES, TRIGGER, TRUNCATE, MAINTAIN`, и те же права выдаются автоматически каждой новой таблице через default privileges схемы `public`. На локальной базе набор шире — полный `ALL` (включая INSERT/UPDATE/DELETE) на 13 таблицах, EXECUTE на 29 функциях из 34 и права на `admin_audit_log_id_seq`; источник тот же, default privileges локального образа (доказано созданием пробной таблицы и функции). RLS от этого не защищает: `truncate products cascade` под ролью `anon` в откаченной транзакции очистил 12 товаров и каскадом `product_brands`, `product_characteristics`, `product_images`, `product_vehicle_types`, `vehicle_hotspots`. Реальная достижимость через публичный Data API на момент находки не подтверждена: PostgREST и pg_graphql не выражают TRUNCATE, роль `anon` — `NOLOGIN`, все изменяющие RPC — SECURITY INVOKER и упирались в RLS, динамического SQL, представлений и таблиц без RLS в схеме нет. Поэтому S1, а не S0: единственным барьером оставался RLS, вне которого TRUNCATE/REFERENCES/TRIGGER/MAINTAIN находятся целиком. | `anon` и `authenticated` имеют ровно `SELECT` на 11 таблицах каталога и `EXECUTE` на двух функциях поиска; TRUNCATE, REFERENCES, TRIGGER и MAINTAIN сняты; внутренние изменяющие RPC доступны только `service_role`; EXECUTE отозван явно у PUBLIC, `anon` и `authenticated`; default privileges не выдают новым таблицам, последовательностям и функциям доступ клиентским ролям. Публичный каталог и серверная админка работают без изменений. | DB grants, default privileges, декларативная схема, pgTAP | 4A | Закрыт |
| QA-014 | S2 | `supabase/schemas/prod.sql` объявлен источником истины, но ведётся руками и разошёлся с миграциями. Из файла пропала таблица `admin_credentials` вместе с тремя её констрейнтами (создана миграцией `20260821171707`, в файл не перенесена), а у `admin_auth_events` было выдумано имя CHECK: в файле значилось `admin_auth_events_key_hash_check`, тогда как в базе констрейнт называется `admin_auth_events_attempt_key_hash_check`. Причина второй ошибки системная: в миграции CHECK объявлен безымянно прямо на колонке (`attempt_key_hash text NOT NULL CHECK (...)`), имя присваивает Postgres по правилу «таблица_колонка_check», и в тексте миграции его нет вовсе — текстовая сверка такую ошибку поймать не может в принципе. На саму базу расхождение не влияло: файл не выполняется. Опасность в том, что он служит входом для генерации миграций, и `supabase db diff` на разошедшемся файле выписал бы миграцию, удаляющую таблицу с хешем пароля администратора. | Файл объявляет ровно те объекты, что есть в базе; расхождение ловится автоматической проверкой, а не глазами; проверка не зависает без объяснения при недоступном Docker. | декларативная схема, скрипты проверки | 4B | Закрыт |

### Важные предварительные параметры

- Размеры и параметры стартовых hero-версий заранее не назначаются: их выбирают по измеренному времени до первого движущегося кадра, устойчивости воспроизведения и визуальному сравнению на mobile/desktop. Прежняя гипотеза 3/5 MB отменена; она не является целью и не разрешает ухудшать master или финальную качественную версию.
- Допустимые bounds для `scale` **не заданы заранее**. Production-распределение значений измеряется в preflight; только после этого выбираются ограничения UI/server/DB и план нормализации outliers.
- Библиотека/sanitizer для SVG **ещё не выбраны**. Нельзя внедрять regex-only sanitizer. Выбор требует проверки поддержки `viewBox`, paths, namespaces, embedded raster, scripts/event handlers и серверной среды.
- Private staging bucket и его lifecycle требуют **отдельного infra approval**. Фаза 3 не означает автоматическое создание/изменение remote Storage.

## Фазы, зависимости и Definition of Done

### Фаза 0 — контракт remediation

Зависимости: baseline `a201d1e`, `PROJECT_BRIEF.md`, подтверждённые кодом находки и явно помеченные риски, требующие browser-аудита.

Результат:

- создан этот живой документ и ссылка из `README.md`;
- зафиксированы постоянные ID, границы, зависимости, DoD, полномочия и release order;
- неизвестные параметры помечены как open questions, а не выданы за решения.

Definition of Done:

- tracked diff проходит `git diff --check`, а новые untracked документы отдельно проверены эквивалентной whitespace-проверкой (обычный `git diff --check` их не охватывает);
- все фазы 0–8, QA ID и обязательные разделы доступны по заголовкам;
- не заявлено исправление открытых проблем или существование browser/E2E coverage;
- diff ограничен этим документом и ссылкой в README.

### Фаза 1 — baseline, preflight и regression harness

Зависимости: фаза 0.

Работы:

- зафиксировать environment preflight: Node/npm, env presence без вывода секретов, доступность local Supabase/Docker и браузеров;
- добавить Playwright с отдельными public/admin projects и безопасным тестовым состоянием;
- smoke: главная, каталог/поиск/пагинация, product route, 404, login failure/rate limit UX, базовый admin CRUD в изолированной среде;
- viewport matrix 320/375/390/768/desktop, console/page errors, keyboard path и скриншотные baseline артефакты;
- добавить regression tests на выявленные дефекты до их исправления, где это practically possible.

Definition of Done:

- документирована команда локального запуска и CI-режим;
- тесты не зависят от production writes и очищают только собственные fixtures;
- есть доказанный failing/pending coverage для QA-002…QA-012 либо явно описанное ограничение;
- `npm run check`, smoke suite и browser artifacts воспроизводимы на поддерживаемой среде.

Статус: **завершена после независимой локальной verification**. QA-001 закрыт; QA-002…QA-012 остаются открытыми и переходят в соответствующие фазы 2–8.

### Фаза 2 — целостность product mutations, reorder и scale

Зависимости: фаза 1; local Supabase/Docker или отдельно согласованная безопасная test DB.

Работы:

- добавить декларативную схему и versioned migrations для атомарных `create_product_with_relations`/`update_product_with_relations`;
- валидировать references, массивы, дубликаты, права вызова, publication side effects и ожидаемую версию (`expected_updated_at`/CAS);
- усилить reorder contracts с явной семантикой полного набора и filtered/page subset;
- измерить production scale values read-only, выбрать bounds, добавить нормализацию и constraints;
- расширить pgTAP: success, rollback на каждой child relation, stale update, duplicate/foreign/missing reorder IDs, grants/RLS.

Definition of Done:

- ни один тестовый failure injection не оставляет частичный товар/связи;
- stale edit не затирает более новую редакцию молча;
- strict reorder отвергает некорректный набор без частичного update;
- bounds подтверждены измерениями, а существующие данные совместимы или имеют проверенную миграцию;
- local reset, pgTAP, app tests и build проходят; remote schema не изменена без approval;
- локальная декларативная схема и миграции согласованы. Remote `schema:check` до отдельно одобренного provisioning может иметь документированный expected drift/pending status и не считается PASS; обычный PASS обязателен после provisioning как release gate.

Статус: **завершена после локальной verification**. QA-002 и QA-003 закрыты. QA-012 закрыт наполовину: bounds измерены и закреплены на трёх уровнях, server-side content validation переходит в фазу 3 вместе с QA-004.

Принятые в фазе решения и осознанные исключения:

- **Границы scale выбраны по измерению, а не назначены.** Read-only `SELECT` к production показал: `product_images.scale` не заполнен ни разу (346 строк), `brands.logo_scale` заполнен у 8 строк в диапазоне 0.75–1.6, `category_brands.logo_scale_override` — у 5 в диапазоне 0.95–1.15; значений ≤ 0 и > 5 нет нигде. Диапазон 0.1–5.0 даёт более чем трёхкратный запас и совместим с обеими средами. Значение вне диапазона отвергается, а не обрезается до границы.
- **Закрыты все три scale-колонки**, хотя QA-012 называет только `product_images.scale`: остальные две имели тот же дефект и вернулись бы отдельной находкой.
- **Все семь reorder RPC приведены к модели слотов.** Прежде их было две несовместимых: `reorder_products` переставлял записи внутри уже занятых значений `order`, остальные присваивали абсолютные `ordinality - 1` и на неполном наборе молча сталкивали подмножество со строками вне набора.
- **`UNIQUE (product_id, "order")` для `product_images` сознательно отложен в фазу 3.** Модель слотов — перестановка уже занятых значений и сама создать дубликат не может; найденный в production дубликат (`korobka-otbora-moschnosti-lkf-atego-2`, две фотографии на `order = 3`) пришёл из пути загрузки, где клиент вычисляет `order` из локального состояния формы. Констрейнт до переписывания этого пути (QA-004) превратил бы устаревшую вкладку в ошибку загрузки, не устранив причину. Миграция при этом нормализует существующий порядок в плотный детерминированный 0..n-1.
- **CAS выполняется по строке, а не по разобранному значению.** `updated_at` хранится с микросекундной точностью; `new Date(...).toISOString()` обрезает до миллисекунд, поэтому разбор версии в `Date` на любом участке пути сделал бы каждое сохранение ложным конфликтом. Проверено на живых данных: `…21.195707+00:00` против `…21.195Z`.
- **`p_expected_updated_at IS NULL` отвергается fail-closed**, иначе устаревшая вкладка, не приславшая поле, обошла бы проверку молча.
- **Ошибки RPC оборачиваются осознанно.** `PostgrestError` в рантайме не является экземпляром `Error` (проверено на живом клиенте), поэтому общий `getErrorMessage` подменял бы текст на generic. Намеренные коды (`22023`, `55000`) доходят до администратора дословно, неожиданные скрываются за общим текстом и уходят в серверный журнал, чтобы имена констрейнтов не утекали в интерфейс.
- **Найдено и исправлено расхождение `supabase/schemas/prod.sql` с миграциями**: `categories.type` значился `NOT NULL` со старым CHECK, хотя миграция `20260819021520` сделала колонку nullable. Исправлено, так как DoD фазы прямо требует согласованности.
- **Браузерных проб для QA-002/QA-003 намеренно нет**: браузер не может ни прервать транзакцию на середине, ни отправить набор идентификаторов, которого интерфейс не формирует. Вместо этого добавлена проба на то, что pgTAP увидеть не может — видимость и текст отказа при конфликте версий (`tests/e2e/admin/product-version-conflict.spec.ts`).

### Фаза 3 — staged direct upload и Storage lifecycle

Зависимости: фазы 1–2; закрытые entry decisions ниже; отдельное одобрение Storage provisioning для remote среды.

Entry decisions до начала implementation:

- TTL для draft, signed upload token и abandoned staging/retention;
- максимальный размер одного файла и максимальное количество файлов на товар/операцию;
- владелец и механизм запуска cleanup/reconciliation (scheduler/runner), retry policy и observability;
- конкретная SVG sanitizer/library, allowlist и server-runtime compatibility; regex-only sanitization запрещена.

Работы:

- создать draft/finalize state machine и private `product-image-staging` contract;
- server выдаёт короткоживущий signed upload token только для scoped path, типа, размера и лимита количества;
- browser загружает каждый файл независимо с progress/retry/cancel;
- finalize сервером скачивает/читает исходный объект, повторно проверяет signature/content/dimensions, безопасно обрабатывает raster и санитизирует допустимый SVG;
- publish/attach выполняется после повторной валидации; cleanup удаляет expired drafts, abandoned staging и orphan files идемпотентно;
- прямой public upload и доверие к client MIME/path запрещены.

Definition of Done:

- create/edit multi-photo flow не передаёт файлы через общий product Server Action payload;
- invalid/oversized/spoofed file не попадает в public bucket/DB;
- DB metadata commit атомарен внутри Postgres; между Storage и Postgres не заявляется общая ACID-гарантия;
- Storage+DB workflow реализован как идемпотентная state machine: объект не становится публичным и не прикрепляется к опубликованному товару до успешной server-side revalidation/finalize;
- retry не создаёт дубликаты, cancel и partial failure понятны пользователю; cleanup/reconcile сходится без дублей и потерь подтверждённых final-объектов;
- lifecycle job и reconciliation имеют метрики/логи и доказанные idempotency tests;
- local `supabase db reset`, pgTAP, Storage lifecycle и permission tests проходят;
- remote `schema:check` до одобренного provisioning может оставаться в документированном expected drift/pending mode; полный PASS обязателен после provisioning как release gate;
- доступ к private staging и public final objects проверен; remote bucket создаётся только после approval.

### Фаза 4 — auth rate-limit reservation до PBKDF2

Зависимости: фазы 1–2; согласованный reservation protocol.

Работы:

- заменить post-verification registration на `begin_admin_auth_attempt` перед PBKDF2 и `finish_admin_auth_attempt` после неё;
- reservation атомарно проверяет window/block/concurrency budget и fail-closed поведение;
- завершение success очищает/обновляет состояние, failure учитывается один раз; abandoned reservations имеют TTL/reconciliation;
- исключить user enumeration и утечки password/IP в logs; constant-time credential handling относится к попыткам, допущенным reservation к PBKDF2. Ранний rate-limit отказ намеренно не выполняет PBKDF2 и может отличаться по времени, поэтому его ответы/сообщения не должны раскрывать валидность credential.

Definition of Done:

- blocked request доказанно не запускает PBKDF2;
- конкурентные попытки не обходят лимит и не double-countятся;
- DB/RPC outage запрещает вход fail-closed с безопасным UX;
- pgTAP + unit + integration tests покрывают boundary, expiry, success, failure и concurrency;
- административный вход и смена пароля работают в разрешённом сценарии.

### Фаза 4A — минимальные права клиентских ролей (внеочередная)

Внеочередная фаза безопасности: находка QA-013 обнаружена во время фазы 4 и по
решению владельца разбирается до фазы 5.

Зависимости: фаза 4 (общая база миграций); отдельного кода приложения не
требует.

Работы:

- read-only аудит фактических прав `anon`, `authenticated` и `service_role` на
  все таблицы, последовательности и функции схемы `public`; сверка локальной
  базы, миграций и `supabase/schemas/prod.sql`; отдельная read-only проверка
  production;
- определение источника лишних прав (baseline, default privileges или
  последующие миграции) и проверка фактической достижимости через настоящий
  Data API с `anon`-ключом, а не только через `SET ROLE`;
- приведение прав к минимально необходимым: `SELECT` на таблицах каталога,
  `EXECUTE` на функциях публичного поиска, всё остальное — только
  `service_role`; TRUNCATE, REFERENCES, TRIGGER и MAINTAIN сняты;
- исправление default privileges, чтобы новые объекты не возвращали проблему;
- сначала правится декларативная схема, затем сверяется соответствующая
  миграция.

Definition of Done:

- `anon` читает опубликованный каталог и пользуется поиском;
- `anon` не может INSERT/UPDATE/DELETE/TRUNCATE ни одну таблицу схемы `public`
  и не может выполнить ни один внутренний изменяющий RPC, причём отказ приходит
  от GRANT, а не от RLS;
- `service_role` сохраняет полный доступ к таблицам и функциям;
- изменение default privileges проверено отдельно на реально созданных
  объектах, а не только по записи в каталоге;
- полный reset базы, pgTAP, `npm run check`, build, public smoke и admin smoke
  проходят.

### Фаза 5 — двухступенчатый hero и бесшовное повышение качества

Зависимости: фаза 1; утверждённый high-quality master; согласованные по таймлайну mobile/desktop derivatives; browser/network measurements и визуальное утверждение перехода.

Работы:

- создать для mobile и desktop лёгкую стартовую и финальную качественную версии из одного master; сохранить одинаковые начало, длительность, частоту кадров и предсказуемые keyframes для синхронизации, master не изменять;
- сохранить boot overlay фиксированным поверх уже смонтированного hero и приоритетно запускать стартовую версию под ним; переход между route-level loading и home boot overlay не должен показывать пустой hero;
- открывать hero не по одному `loadeddata`, а только после успешного `play()`/события `playing` и подтверждения реально отправленного в compositor движущегося кадра (`requestVideoFrameCallback` с совместимым fallback);
- растворять overlay за 500–600 мс без layout shift: фон уходит по opacity, logo/progress получают только лёгкие scale/translate и ограниченный blur; полноэкранный blur поверх видео не применять;
- после начала стартового воспроизведения всегда запрашивать качественную версию, не останавливая стартовую; после decode/buffer синхронизировать `currentTime` и выполнить короткий crossfade двух наложенных video layers без замены `src`, перезапуска или вспышки;
- скорость соединения определяет только длительность показа стартовой версии: качественная версия остаётся целевым состоянием. При её ошибке стартовая продолжает движение; при невозможности стартового autoplay/media нужен визуально завершённый fallback и понятный retry/play recovery без бесконечного loader;
- сохранять pause/resume при скрытой вкладке и полностью ушедшем из viewport hero без лишнего reload; подобрать codecs/CRF/resolution по измерениям первого движения, LCP, transfer, dropped frames, CPU/GPU и качества loop/crossfade.

Definition of Done:

- в штатном autoplay-сценарии overlay начинает исчезать только когда стартовая версия уже воспроизводится и движущийся кадр отрисован; после растворения нет пустого, чёрного или неподвижного состояния, а геометрия hero не меняется;
- на медленном соединении стартовая версия продолжает воспроизводиться без паузы, пока качественная догружается; сеть не является условием отказа от последующего повышения качества;
- переход к качественной версии синхронизирован и не показывает restart, flash, seek jump, двойной контур или заметную ступень качества; ошибка качественной версии оставляет рабочую стартовую;
- отказ autoplay/обеих media versions не держит сайт за бесконечным loader и не показывает пустой фон: fallback остаётся визуально завершённым, а recovery не зацикливается;
- premium dissolve проверен на mobile/desktop: 500–600 мс, без fullscreen blur, dropped-frame всплеска, layout shift и изменения существующей композиции hero/header/content;
- quality review не выявляет blur, halos, banding, broken loop или distorted details; performance traces фиксируют время до первого движущегося кадра, LCP, transfer обеих ступеней, dropped frames и стоимость decode/composite;
- финальные параметры обеих ступеней записаны по измерениям и визуальному сравнению, а не по прежнему произвольному лимиту 3/5 MB.

#### Измеренное состояние baseline (read-only, 2026-08-27)

Замер выполнен без единого изменения: код, видео, Supabase и заголовки не трогались; к production были только чтения. Требования фазы 5 ниже/выше не изменялись — это фактическая база для их выполнения.

| Файл | Разрешение | Кодек | Длительность | Размер | Битрейт |
|---|---|---|---|---:|---:|
| `hero-background-desktop.mp4` | 2560×1440 | H.264 High L5.0 | 25,42 с | 25 544 496 B | 8,04 Мбит/с |
| `hero-background-mobile.mp4` | 1440×2560 | H.264 High L5.0 | 25,42 с | 22 588 953 B | 7,11 Мбит/с |

Обе версии: 24 fps, без аудиодорожки, ключевые кадры каждые 2 с (13 на ролик), `moov` перед `mdat` (faststart включён). Production отдаёт побайтово те же файлы, `Accept-Ranges: bytes`, но с `Cache-Control: no-cache` — повторный визит перепроверяет ~25 МБ.

| Сеть | Версия | Первый декодированный кадр | Первое движение |
|---|---|---:|---:|
| без ограничения | desktop | 3,02 с | 3,10 с |
| без ограничения | mobile | 0,26 с | 0,30 с |
| ~1,5 Мбит/с | desktop | 4,62 с | не наступило (25 с) |
| ~1,5 Мбит/с | mobile | 2,90 с | не наступило (25 с) |

Разница desktop/mobile без ограничения объясняется прогревом CDN: desktop запрашивался первым, холодным.

**Подтверждённая проблема неподвижного кадра.** Отдельное наблюдение 60 с на ~1,5 Мбит/с (mobile): `loadeddata` и `canplay` на 3,75 с, буфер набрал 13,08 с видео, но событие `playing` **не наступило**, отрисован ровно **1** кадр, `currentTime` остался 0,00 из 25,42. Причина арифметическая и не зависит от эвристик браузера: поток требует 7–8 Мбит/с при доступных ~1,5 Мбит/с — дефицит в 4,7–5,4 раза; полная загрузка 25,5 МБ заняла бы ≈136 с. Дефект QA-006 подтверждён и оказался строже формулировки реестра: на медленной сети hero остаётся неподвижным закономерно, а не только при срабатывании аварийного таймера.

Со стороны приложения подтверждено чтением кода: заставка открывается по `loadeddata` (первый декодированный кадр), а не по подтверждённому воспроизведению; дополнительно `HERO_BOOT_SAFETY_TIMEOUT_MS = 5000` открывает её независимо от состояния видео.

**Master найден и проверен (обновление 2026-08-27).** Прежняя запись «master отсутствует» была неверна: файла нет в рабочем дереве, но он есть в истории как указатель Git LFS, а сам объект лежит локально в `.git/lfs/objects`. Проверено рабочей копией вне репозитория (внутренний файл LFS не изменялся и не перемещался):

| Параметр | Значение |
|---|---|
| SHA-256 | `40433c58…e97be175` — совпал с ожидаемым |
| Размер | 404 945 384 B |
| Разрешение | 3840×2160 (16:9) |
| Кодек | H.264 High L5.2, yuv420p, bt709, progressive |
| Частота кадров | 24 fps постоянная |
| Длительность / кадры | 25,416667 с / 610 кадров, `start_time` 0,000000 |
| Битрейт | ≈127,5 Мбит/с |

**Происхождение derivatives подтверждено документально**, а не по сходству: указатель LFS для `public/videos/hero-background-desktop.mp4` и `hero-background-mobile.mp4` на коммите перед `e888882` («perf(hero): re-encode background video as true 2K») содержит ровно этот OID и размер. Оба указателя одинаковы — отдельного портретного master нет, мобильная рамка получается центральным кропом. Таймлайн совпадает точно: у master и у обеих 2K-версий 610 кадров, 25,416667 с, 24 fps, `start_time` 0.

**Уточнение к строке о ключевых кадрах выше.** Сетка «раз в 2 секунды» выдерживается только до 14 с. Далее у обоих файлов есть кадры на 15,958333 и 17,916667 с, а после этого списки расходятся: desktop — 19,666667 / 21,666667 / 23,666667 (13 кадров всего), mobile — 19,125 / 21,125 / 23,125 / 25,0 (14 кадров). Причина — принудительные ключевые кадры на плавных переходах при `-keyint_min 48` без `-sc_threshold 0`. Практическое следствие для фазы 5: синхронная замена возможна не в произвольный момент, а только в точках, общих для стартовой и качественной версии; общими у desktop и mobile остаются 0, 2, 4, 6, 8, 10, 12, 14, 15,958333 и 17,916667 с. Поскольку замена по замыслу происходит рано, ранних общих точек достаточно; стартовые варианты изготавливаются с ключевыми кадрами, точно повторяющими список соответствующей качественной версии.

**Живая проверка выбранной пары (2026-08-27).** Владелец предварительно утвердил D5 (1280×720, 2,68 МБ) для desktop и M5 (720×1280, 2,53 МБ) для mobile. Проверка выполнена локально в Chromium 151 через Playwright, чистый контекст на каждый прогон (`Network.clearBrowserCache`, `setCacheDisabled`, `no-store` от сервера, уникальный параметр адреса — три независимых гарантии «первого визита»). Атрибуты `<video>` повторяют Hero буква в букву: `autoplay muted loop playsinline preload="metadata"`, без poster. Скорость режется **на стороне сервера** (проверено curl: 186 318 Б/с при цели 187 500), а не через DevTools: троттлинг CDP на localhost применяется не во всех сборках, и его молчаливое отсутствие обесценило бы весь замер.

| Прогон | Первый кадр | Первое движение | `playing` | Кадров за 40 с | Провалы >400 мс | Итог |
|---|---:|---:|---:|---:|---:|---|
| D5 без ограничения | 0,06 с | 0,10 с | 0,05 с | 24,2 к/с | нет | норма |
| M5 без ограничения | 0,04 с | 0,11 с | 0,05 с | 24,2 к/с | нет | норма |
| D5 при 1,5 Мбит/с | 0,52 с | **0,55 с** | 0,52 с | 23,9 к/с | нет | круг доигран, зациклился |
| M5 при 1,5 Мбит/с | 0,51 с | **0,54 с** | 0,52 с | 23,9 к/с | нет | круг доигран, зациклился |
| D5 при 1,0 Мбит/с | 0,78 с | **0,82 с** | 0,77 с | 23,8 к/с | нет | круг доигран, зациклился |
| M5 при 1,0 Мбит/с | 0,74 с | **0,79 с** | 0,76 с | 23,7 к/с | нет | круг доигран, зациклился |
| *контроль* desktop 2K при 1,5 | 4,52 с | **не наступило** | не наступило | 1 кадр за 40 с | — | `currentTime` 0,000 |
| *контроль* mobile 2K при 1,5 | 2,77 с | **не наступило** | не наступило | 1 кадр за 40 с | — | `currentTime` 0,000 |
| *контроль* desktop 2K при 1,0 | 6,77 с | **не наступило** | не наступило | 1 кадр за 40 с | — | `currentTime` 0,000 |
| *контроль* mobile 2K при 1,0 | 4,15 с | **не наступило** | не наступило | 1 кадр за 40 с | — | `currentTime` 0,000 |

Контрольные прогоны текущих 2K-версий добавлены намеренно: они доказывают, что ограничение скорости реально действовало, и воспроизводят подтверждённый дефект QA-006 один в один — ровно один кадр за 40 секунд, воспроизведение не начинается.

**Событие `waiting` в прогонах D5/M5 — граница цикла, а не нехватка канала.** Оно приходит на 25,9–26,2 с, когда ролик длиной 25,417 с доигран и `loop` перезапускает его с нуля; буфер к этому моменту уже содержит весь файл (`suspend` на 14,1–22,5 с), восстановление занимает 20 мс. Единственная настоящая дозагрузка — `waiting@1,00 с` в D5 при 1,0 Мбит/с, тоже с восстановлением за 20 мс. Провалов показа длиннее 400 мс нет ни в одном из шести прогонов.

**Границы этой проверки.** Измерялись файлы, а не будущий двухступенчатый Hero: стенд повторяет атрибуты `<video>`, но не содержит загрузочной заставки, IntersectionObserver и явного `play()` с повторами. Отдача шла с локального сервера, без TLS и CDN. Совпадение симптома контрольных прогонов с ранее снятым замером по production подтверждает, что стенд отражает реальное поведение.

#### Реализация двухступенчатого hero (2026-08-27)

Владелец разрешил загрузку в Storage и правку Hero. Стартовая ступень собрана скриптом проекта из проверенного master и лежит в **новой** версионированной папке `site-media/hero/2026-08-27-startup/` (2 810 484 и 2 650 542 B); качественная ступень не перезаписывалась — сверено, `hero/2026-08-18-2k/hero-background-desktop.mp4` по-прежнему 25 544 496 B. Ключевые кадры стартовой ступени не выписаны руками: скрипт читает их из файла качественной ступени, форсирует те же времена и после кодирования сверяет совпадение, иначе падает.

**Момент подмены пришлось искать в три захода, и первые два дали ложные решения.** Это стоит зафиксировать: оба выглядят правдоподобно и оба неверны.

| Критерий | Что произошло |
|---|---|
| `canplaythrough` качественной ступени | Оценка браузера оптимистична. На 1,5 Мбит/с подмена случалась на 131-й секунде при недокачанном файле, и дальше картинка рвалась почти непрерывно: **19 провалов показа и 19 событий ожидания** за 48 секунд |
| «файл целиком в буфере» | Не наступает никогда. Приостановленному `<video>` Chrome докачивает **15,23 с из 25,42** и останавливается до начала воспроизведения; за 60 секунд наблюдения буфер не сдвинулся, подмена не происходила вовсе |
| измеренная скорость загрузки | Работает. Считается, сколько секунд видео приезжает за секунду реального времени, начиная с момента запроса файла; порог — полуторный запас при накопленных 10 с буфера |

Отсчёт скорости начинается в момент `load()`, а не с первого события `progress`: на быстром канале весь прирост буфера происходит одним махом до первого замера, и измерение «между событиями» давало нулевую скорость — подмена не наступала и на гигабитном канале.

**Живая проверка собранной страницы** (Chromium 151, production build, чистый контекст, ограничение через CDP — файлы на удалённом Storage, где троттлинг DevTools применяется штатно):

| Условие | Стартовая ступень | Заставка ушла | Качественная запрошена | Подмена | Провалы показа после подмены |
|---|---|---|---|---|---|
| без ограничения | движение 0,29–0,46 с | 0,57 с | 0,34 с | **1,81 с** | нет; ролик доигран и зациклен |
| 1,5 Мбит/с | движение 6,3–6,9 с | 6,6 с | 23,2 с | не выполнена (намеренно) | стартовая играет ровно, 0 провалов |
| 1,0 Мбит/с | движение 9,5–10,6 с | 9,7 с | 33,7 с | не выполнена (намеренно) | 3 короткие заминки в первые 19 с, дальше ровно |

Порядок запросов проверен отдельно: в отданном сервером HTML объявлена только стартовая ступень, адрес качественной проставляется кодом — первым `.mp4`-запросом во всех прогонах идёт `hero/2026-08-27-startup/`.

**Трактовка требования.** Реестр говорит «качественная версия всегда догружается в фоне и бесшовно заменяет стартовую», а DoD — «crossfade-ится без остановки». Когда канал физически не тянет 8 Мбит/с, эти два требования противоречат друг другу, и приоритет отдан второму: файл догружается всегда, но подмена выполняется только при доказанном запасе скорости. На медленном канале посетитель остаётся на стартовой ступени — это и есть «медленная сеть влияет только на длительность стартового качества».

#### Доработка после первого прогона (2026-08-27, вторая итерация)

Владелец указал на сценарий, до которого проверки не доходили: вкладку прячут ПОСЛЕ `quality.play()`, но ДО первого отрисованного кадра качественной ступени. Разбор подтвердил дефект и вскрыл рядом второй, более опасный.

**Дефект 1 — потерянная подмена.** Пауза в этом промежутке означает, что кадров больше не будет, значит `swap-finished` не наступит никогда: состояние навсегда оставалось `swapping`, а возобновление выбирало стартовую ступень. Посетитель молча оставался на 720p до перезагрузки страницы. Исправлено событием `swap-interrupted` (`swapping → ready`) и повтором подмены при возвращении контекста; файл к этому моменту уже скачан, поэтому повторяются только выравнивание и запуск. Откат общий для трёх путей: `visibilitychange`, `pagehide`/`pageshow` и уход hero с экрана.

**Дефект 2 — отказ ПОСЛЕ подмены, найден живой проверкой.** Оборванный на середине файл ломается не сразу: заголовок целый, длительность объявлена, весь ролик числится в буфере, измеренная скорость высокая — подмена проходит штатно, и лишь дойдя до обрыва воспроизведение падает с `MEDIA_ERR_DECODE`. Стартовая ступень к этому моменту уже погашена, и hero замирал на мёртвом кадре. Требование фазы «отказ качественной ступени оставляет стартовую рабочей» до этого выполнялось только ДО подмены. Исправлено: `done` перестал быть терминальным для отказа, и при ошибке качественной ступени стартовая возвращается — она загружена целиком и стоит на паузе там, где её оставили, поэтому возврат идёт без новых запросов.

| Проверка | Результат |
|---|---|
| Аномалия гидрации | **не воспроизведена**: 18 холодных запусков (12 + 6, чередуя компьютер и телефон, свежий контекст, очищенный и отключённый кэш) — во всех гидрация состоялась, заставка ушла, качественная ступень запрошена. Единственные ошибки консоли — локальный 404 `_vercel/speed-insights`, к приложению отношения не имеющий |
| Отказ запроса качественной ступени | стартовая играет, подмены нет, заставка ушла |
| Ответ 500 | то же; `MEDIA_ERR_SRC_NOT_SUPPORTED`, подмены нет |
| Оборванный ответ (30 % файла) | до исправления hero замирал; после — подмена происходит, на обрыве ловится ошибка, стартовая возвращается и играет дальше |

**Метрики (локальный production-сервер, Chromium 151, программная отрисовка):**

| Условие | LCP | CLS | Потеряно кадров |
|---|---:|---:|---|
| компьютер, без ограничения | 464–504 мс | 0 | стартовая 3–7 из 82–169; качественная 2 из 547 |
| компьютер, 1,5 Мбит/с | 1028–1132 мс | 0 | стартовая 7–9 из 930 |
| телефон, без ограничения | 460–504 мс | 0 | стартовая 6–7 из 116–200; качественная 4–7 из 319–498 |
| телефон, 1,5 Мбит/с | 1088–1144 мс | 0 | стартовая 7 из 910 |

LCP-элементом оказывается логотип загрузочной заставки. Сдвига макета нет ни в одном прогоне: обе ступени лежат в одном `fixed`-слое фиксированного размера. Доля потерянных кадров завышена средой: headless-Chromium рисует программно (SwiftShader), без аппаратного декодирования.

**Устойчивость подмены.** В отдельной серии из трёх прогонов на быстром канале подмена наступала на 3,6 / 4,1 / 7,2 с; в более ранних измерениях — на 1,8 с. Один прогон из восьми (при загруженном процессоре сразу после серии холодных запусков) не дошёл до подмены за 25 с и корректно остался на стартовой ступени. Разброс объясняется тем, что порог требует накопленных 10 с буфера, а темп его накопления зависит от загрузки машины.

**Что остаётся непроверенным.** Два случая в LCP-замерах на телефоне не дали ни одной записи `largest-contentful-paint` — это особенность измерения, а не страницы (в соседних прогонах запись есть). Проверки выполнялись на локальном production-сервере; deploy не выполнялся.

#### Сторож зависшей качественной ступени (2026-08-27, третья итерация)

Оставшаяся дыра закрыта до публикации: возврат к стартовой ступени был привязан только к событию `error`, а поток может встать и **без ошибки** — соединение открыто, новые байты не идут, браузер ждёт. Ошибки нет, и hero замирает на кадре, который браузер считает исправным.

Отсчёт ведётся **не от `play()`**, а от последнего признака движения: показанный кадр (`requestVideoFrameCallback`), а где его нет — сдвиг `currentTime`. Абсолютный таймаут был бы неверен: он сработал бы и на исправном видео, которое просто долго играет. Порог — 5 с; при срабатывании качественная ступень терминально переходит в `failed`, скрывается и останавливается, а уже загруженная 720p возвращается без повторной попытки 2K. При скрытии вкладки, `pagehide` и уходе hero за экран сторож приостанавливается, и штатная пауза зависанием не считается; при возвращении отсчёт начинается заново, а не досчитывает простой.

**Побочно найден и закрыт второй дефект.** Живая проверка показала, что решение о подмене могло не приниматься вовсе: если весь буфер приезжает быстрее, чем набирается минимальная выборка для замера скорости, все события `progress` укладываются в это окно, а `suspend` не приходит — ответ ведь не завершён. Замерено: 20 МБ за 258 мс, после чего событий больше нет и подмена не наступает никогда. Добавлен опрос готовности раз в 500 мс, который прекращается сам, как только стадия загрузки пройдена.

**Живая проверка зависания.** Локальный HTTPS-сервер отдаёт 60 % файла и не закрывает соединение: заголовки обещают полный размер, сетевой ошибки нет, `video.error` пуст. Наблюдение: подмена происходит штатно на 6-й секунде, качественная ступень играет до 13,77 с и останавливается без ошибки; через пять секунд неподвижности она гаснет и встаёт на паузу, а стартовая возвращается и продолжает играть (3,12 → 6,13 → 9,14 с). Range-запросы в стенде поддержаны намеренно: без них Chrome буферизует около четырёх секунд, подмена не наступает и проверять было бы нечего.

**Тесты.** Модульных проверок сторожа девять; в компоненте — постоянное зависание, короткая пауза меньше порога, сброс отсчёта возобновившимся кадром, скрытая вкладка, `pagehide`, ушедший с экрана hero и оба порядка совпадения ошибки со сторожем. Регресс-доказательства: выключение сторожа роняет 4 проверки, снятие приостановки — 3, снятие возобновления — 1.

**Проба QA-006 в e2e переписана.** Прежняя описывала отменённую концепцию (poster-first, без `autoplay`, ноль запросов) и держалась на `test.fail`, то есть проверяла требование, которого больше нет. Теперь проверяется утверждённое: в отданном сервером HTML объявлена только лёгкая ступень, первым видеозапросом идёт она, заставка уходит быстрее аварийного таймера. Заодно исправлен стенд: `stubLocalHeroVideo` подставлял пустое тело, из-за чего движение не наступало и заставку снимал аварийный таймер — проверка проходила, но проверяла не то, что заявлено. Теперь отдаётся настоящий локальный файл лёгкой ступени (тяжёлая по-прежнему заглушается: 22–25 МБ через перехват на каждой навигации давали оборванные запросы).

**Нестабильность публичного набора.** Падения перемещаются между прогонами и приходятся на страницы без hero (каталог, карточка товара), а при повторе проходят. Наблюдались две причины: `net::ERR_ABORTED` на навигации (та же подпись, что уже записана как внешний блокер QA-007) и предупреждение драйвера `GPU stall due to ReadPixels` от программной отрисовки в headless-Chromium. Целевая проба QA-006 проходит во всех прогонах. Эти страницы в рамках QA-006 не исправлялись.

**Статус реализации.** QA-006 остаётся `Открыт` до выкладки. Код, тесты и файлы готовы; `Cache-Control` не трогался — Storage по-прежнему отдаёт `no-cache`, в том числе на новой загрузке, где скрипт запрашивал год.

### Фаза 6 — SEO metadata и global 404

Зависимости: фаза 1; релевантная документация установленной Next.js 16 прочитана до кода.

Работы:

- включить `meta_title`/`meta_description` в public product contract;
- централизовать title/description/canonical/OpenGraph fallback и sanitization;
- добавить полный global not-found для нескольких root layouts и необходимую Next config опцию;
- сохранить сегментные 404 там, где они дают более точный контекст.

Definition of Done:

- custom SEO values и fallback values проверены unit/E2E тестами;
- unknown public/admin/root URL возвращает ожидаемый 404 и русскоязычный документ без runtime error;
- canonical стабилен, секретные/draft данные не попадают в metadata;
- `npm run check`, build и browser tests проходят.

**Статус реализации.** QA-007 и QA-008 закрыты; фаза 6 завершена. Неизвестные root, public и admin URL обрабатываются `app/global-not-found.tsx` на уровне роутера и возвращают HTTP 404 с единым русскоязычным документом. Сегментная public-404 сохранена для контекстных вызовов `notFound()` внутри существующих маршрутов.

### Фаза 7A — accessibility без намеренных визуальных изменений

Зависимости: фаза 1.

Работы:

- добавить корректные `aria-expanded`, `aria-controls`, ids и accessible labels;
- Escape закрывает menu/dialog/sheet, focus возвращается opener, tab order предсказуем;
- login получает `<main>`, BackButton — безопасный fallback при отсутствии полезной history;
- стабильная неанимированная hit-wrapper отделяется от animated/dragged visual node там, где теряются clicks;
- focus-visible сохраняется и не скрывается.

Definition of Done:

- keyboard-only сценарии проходят в реальном браузере;
- automated accessibility scan не имеет новых serious/critical violations;
- screen-reader semantics проверены инспекцией accessibility tree;
- desktop screenshots не имеют непредусмотренных визуальных отличий;
- timing, easing и характер существующих анимаций не изменены.

**Статус реализации.** QA-009 закрыт; фаза 7A завершена. Desktop dropdown, главное mobile-меню и вложенные mobile disclosure получили устойчивые ids и `aria-controls`; Escape закрывает desktop dropdown с возвратом фокуса, а на mobile первый Escape сворачивает вложенный раздел и второй — всё меню. Accessible labels отражают открытое/закрытое состояние, keyboard focus закреплён явным `focus-visible` ring. Login содержит ровно один `<main>`. `BackButton` сохраняет native history и scroll restoration только при подтверждённой безопасной внутренней записи; прямой, bookmarked или cross-origin вход заменяется на предсказуемый fallback (`/catalog`, а для корня каталога — `/`). Пункт про hit-wrapper не потребовал изменения: в scope QA-009 не обнаружено animated/dragged control с потерей clicks; подтверждение таких дефектов остаётся условной работой QA-010. Визуальные размеры, расположение и motion не менялись.

### Фаза 7B — контролируемый mobile reflow

Зависимости: фазы 1 и 7A.

Работы:

- сначала воспроизвести и зафиксировать browser evidence, затем исправлять только подтверждённые overflow/overlap на 320–380 px через wrap/stack/min-width/containment;
- сохранить понятные touch-targets около 44 px и safe-area spacing;
- проверить формы, admin lists, bulk actions, header, каталог, product gallery/carousel и WhatsApp badge;
- desktop layout менять только если это непосредственно устраняет дефект.

Definition of Done:

- на 320, 375, 390, 768 и desktop нет непреднамеренного horizontal scroll, clipping или overlap;
- все критические controls достижимы touch и keyboard;
- before/after screenshots рассмотрены, разрешённые отличия перечислены;
- нет изменений цветов, typography system, content hierarchy или motion language.

### Фаза 8 — deterministic build и code hygiene

Зависимости: функциональные фазы 2–7 завершены; релевантная local Next documentation прочитана.

Работы:

- использовать локальный font asset/API и исключить build-time/runtime network dependency шрифта;
- задать `poweredByHeader: false`;
- согласовать `.nvmrc`, `engines`, CI и фактически поддерживаемые Node/npm versions;
- добавить прямую зависимость `@next/env`, если проект импортирует её напрямую;
- удалить unused/archive code только после `rg`, import graph и build evidence; сохранить согласованные архивные ресурсы;
- clean install и build должны давать одинаковый результат без незафиксированных генераций.

Definition of Done:

- `npm ci`, `npm run check`, `npm run build`, relevant schema/browser tests проходят из чистого checkout;
- нет незаявленного font fetch и `X-Powered-By` header;
- lockfile соответствует manifest, Node contract документирован;
- список удалённых/сохранённых файлов зафиксирован; пользовательские ресурсы не затронуты.

## Traceability: находка → доказательство → закрывающая фаза

QA ID переводится в `Закрыт` только после получения указанного acceptance evidence. Завершение работ по фазе без этого доказательства не закрывает находку.

| QA ID | Acceptance evidence / тест | Закрывающая фаза |
|---|---|---:|
| QA-001 | Playwright config и изолированные public/admin smoke запускаются документированной командой; browser artifacts, console/page-error checks и CI/local mode воспроизводимы. | 1 |
| QA-002 | pgTAP failure injection для каждой child relation подтверждает полный rollback; integration tests подтверждают success и CAS stale-update rejection. | 2 |
| QA-003 | pgTAP/RPC tests отклоняют duplicate, foreign, unknown и недопустимо missing IDs без partial update; допустимый full/subset contract проверен отдельно. | 2 |
| QA-004 | Browser/integration flow загружает несколько файлов независимо; content revalidation, idempotent finalize/retry/cancel и cleanup/reconcile tests доказывают отсутствие преждевременной публикации, дублей и потери подтверждённых final-объектов. | 3 |
| QA-005 | pgTAP + integration concurrency tests доказывают reservation до PBKDF2, отсутствие hash work для blocked request, single accounting, TTL и fail-closed outage. | 4 |
| QA-006 | Throttled browser/network evidence на mobile/desktop подтверждает: startup-video запрашивается первой; overlay ждёт `playing` и реально отрисованный движущийся кадр; после dissolve нет blank/static state; high-quality tier догружается после старта, синхронно crossfade-ится без остановки, а его отказ оставляет startup-tier рабочим. Отдельно измерены first moving frame, LCP, transfer и dropped frames. | 5 |
| QA-007 | Unit/E2E tests подтверждают custom и fallback metadata, canonical и отсутствие draft/secret data в публичном HTML. | 6 |
| QA-008 | Browser/E2E для неизвестных public/admin/root URL подтверждает русскоязычный 404-документ, корректный status и отсутствие runtime errors. | 6 |
| QA-009 | Keyboard-only сценарии, accessibility tree и automated scan подтверждают disclosure relationships, Escape/focus return, `<main>`, BackButton fallback и visible focus. | 7A |
| QA-010 | Сначала сохранены browser evidence и baseline screenshots конкретных дефектов; затем before/after matrix 320/375/390/768/desktop подтверждает исправление только воспроизведённых overflow/overlap/click issues без desktop/motion regression. | 7B |
| QA-011 | Чистый `npm ci` + check/build проходит на согласованной Node version; нет font network fetch и `X-Powered-By`, lockfile/manifest согласованы, удаления подтверждены import graph. | 8 |
| QA-012 | Фаза 2 доказывает measured scale bounds и UI/server/DB constraints; фаза 3 доказывает server-side content validation/sanitization и rejection spoofed formats. ID закрывается только после прохождения обоих наборов evidence. | 3, после 2 и 3 |
| QA-013 | pgTAP по фактическому каталогу прав доказывает: у `anon`/`authenticated` нет INSERT/UPDATE/DELETE/TRUNCATE/TRIGGER/REFERENCES/MAINTAIN ни на одной таблице `public`, нет доступа к служебным таблицам и последовательностям, нет EXECUTE ни на одной внутренней функции и ни одна функция не выдана PUBLIC; `service_role` сохраняет полный доступ. Живые проверки под самой ролью подтверждают отказ по GRANT (`42501` с текстом `permission denied`), а не по RLS. Default privileges проверяются на реально созданных таблице, последовательности и функции. Через настоящий Data API с `anon`-ключом подтверждено чтение каталога и поиска и отказ на записи и внутренних RPC. | 4A |
| QA-014 | `npm run schema:declarative` сверяет имена объектов файла с фактической схемой локальной базы, снятой официальным `supabase db dump`, и проходит только при полном совпадении. Проверка доказана обоими способами: на согласованной схеме проходит, при удалении таблицы и при переименовании одного констрейнта падает с перечислением расхождений. Недоступность Docker или незапущенный стек дают понятную диагностику и ненулевой код возврата вместо зависания. | 4B |

## Матрица проверок

| Проверка | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7A/7B | 8 | Перед релизом |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| Tracked `git diff --check` + отдельная проверка untracked | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `npm run lint` / `typecheck` / `test` | — | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `npm run build` | — | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Playwright public smoke | — | создать | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Playwright admin smoke | — | создать | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 320/375/390/768/desktop screenshots | — | baseline | по риску | upload UI | login | hero startup/quality transition | 404/product | обязательно | итог | обязательно |
| Keyboard/a11y tree/automated scan | — | baseline | по риску | upload UI | login | hero playback/recovery controls | 404 | обязательно | итог | обязательно |
| `supabase db reset` + pgTAP | — | preflight | обязательно | обязательно | обязательно | — | по схеме | — | — | обязательно |
| `npm run schema:check` (remote) | — | preflight | pending допустим до approval | pending допустим до provisioning | pending допустим до approval | — | если schema | — | — | PASS после provisioning |
| DB grants/RLS/advisors | — | inventory | обязательно | обязательно | обязательно | — | при изменениях | — | — | обязательно |
| Storage permission/lifecycle tests | — | baseline | — | обязательно | — | — | — | — | — | обязательно |
| Media visual/performance inspection | — | baseline | — | upload previews | — | обязательно | — | motion regression | — | обязательно |
| Production smoke/observability | — | — | только после release | только после release | только после release | только после release | только после release | только после release | только после release | обязательно |

`—` означает «не обязательна именно для этой фазы», а не запрет на запуск. Любое изменение с более широким риском расширяет набор проверок. Для фаз со схемой локальные `supabase db reset`, pgTAP и проверки permissions/lifecycle обязательны. Remote `schema:check`, который видит ещё не provisioned изменения, фиксируется как документированный expected drift/pending, а не маскируется под PASS; обычный PASS обязателен после отдельного approval и provisioning как release gate.

## Контролируемые визуальные изменения 7A/7B

### 7A — визуально нейтральные изменения

Разрешены DOM/semantic wrappers, ARIA-атрибуты, focus management, Escape handling, стабильная hit-area и `<main>`. Не разрешены изменения palette, typography, spacing system, animation timing/easing и desktop composition. Focus-visible может стать заметнее только там, где существующее состояние не удовлетворяет keyboard accessibility.

### 7B — видимый, но ограниченный reflow

После воспроизведения конкретного дефекта на 320–380 px разрешены перенос строк, stack вместо row, увеличение слишком маленькой области касания и safe-area отступ. Само наличие риска QA-010 не разрешает массовую перекомпоновку: изменения вносятся только для подтверждённых browser-аудитом случаев. Любое отличие на desktop должно иметь прямую связь с зарегистрированным дефектом. До/после фиксируются скриншотами на 320, 375, 390, 768 и desktop; непредусмотренное отличие считается регрессией.

## Полномочия и запреты

Локальная реализация, тесты, декларативная схема и ещё не применённые миграции допустимы в рамках отдельно запущенной фазы. Следующие действия **не разрешены этим планом** и требуют отдельного явного разрешения пользователя:

- `git push`, PR merge, deployment или promotion в production;
- применение миграций к remote/production Supabase;
- создание, удаление или изменение remote Storage buckets/policies/lifecycle;
- изменение DNS, Cloudflare, доменов или Vercel project settings;
- destructive reset, удаление production data/objects или ротация secrets.

Секреты, реальные пароли, private keys и raw IP не выводятся в документацию, логи тестов или отчёты.

## Checkpoints и rollback

После каждой средней/крупной фазы:

1. повторно проверить `git status` и отделить пользовательские изменения;
2. выполнить матрицу проверок этой фазы;
3. обновить Graphify, если граф доступен;
4. обновить реестр, decision log, limitations и changelog;
5. создать локальный result-focused Git checkpoint; не push автоматически.

Rollback выполняется checkpoint-by-checkpoint. Для DB используется новая forward migration, а не переписывание уже применённой history. Storage rollout обязан быть backward-compatible: старый public upload flow не удаляется до проверки нового finalize/reconciliation. Для auth сначала выкатываются совместимые DB RPC, затем app; прежний RPC удаляется только отдельной поздней миграцией. Media derivatives и config включаются обратимо; master не удаляется. Не использовать `git reset --hard` или destructive remote commands для отката.

## Decision log

| Дата | Решение | Причина | Статус |
|---|---|---|---|
| 2026-08-24 | Вести remediation в фазах 0–8 с локальным checkpoint после средней/крупной фазы. | Ограничить blast radius и сделать доказательства проверяемыми. | Принято |
| 2026-08-24 | Product create/update переносить в атомарные Postgres RPC с CAS для update. | Компенсирующие запросы приложения не дают настоящей транзакции. | Принято к реализации |
| 2026-08-24 | Multi-photo create строить как draft + private staged direct upload + server finalize. | Убрать общий multipart payload/CPU bottleneck и сохранить server trust boundary. | Принято к реализации; infra не одобрена |
| 2026-08-24 | Auth использовать строгий reservation до PBKDF2, без per-process fallback. | Blocked traffic не должен расходовать hash budget; serverless memory не security boundary. | Принято к реализации |
| 2026-08-27 | Hero делать двухступенчатым: сначала гарантированное движение лёгкой startup-версии под boot overlay, затем обязательное бесшовное повышение до качественной версии; master сохранять. | Зафиксированное продуктовое требование — при растворении loader видео уже проигрывается, а ускорение не создаёт пустой/статичный hero и не отменяет стремление к максимальному качеству. | Принято к реализации |
| 2026-08-24 | Accessibility разделить на визуально нейтральную 7A и контролируемую mobile 7B. | Не смешивать исправление поведения с редизайном. | Принято |
| 2026-08-27 | QA-013 получает severity **S1, а не S0**. | Через публичный Data API находка на момент разбора не эксплуатируется: PostgREST и pg_graphql не выражают TRUNCATE, роль `anon` — `NOLOGIN`, все изменяющие RPC — SECURITY INVOKER и упираются в RLS, динамического SQL и объектов без RLS в схеме нет. Но RLS оставался единственным барьером, а TRUNCATE/REFERENCES/TRIGGER/MAINTAIN находятся вне него целиком, поэтому любое будущее ослабление политики или таблица без RLS давали бы немедленный полный доступ. | Принято |
| 2026-08-27 | Default privileges правятся двумя записями: схемной и **глобальной** `ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC`. | Итоговые права нового объекта Postgres считает объединением глобальной и схемной записи, а при отсутствии глобальной её место занимает встроенное умолчание, где у функций есть EXECUTE для PUBLIC. Проверено: одной схемной записи недостаточно — новая функция получала ACL `{=X/postgres,...}`. Побочный эффект осознан: временные функции `pg_temp`, создаваемые ролью `postgres`, тоже перестают быть доступны другим ролям, поэтому два pgTAP-теста получили явный `grant execute` на своих помощников. | Принято |
| 2026-08-27 | Номер QA-013 закреплён за регрессией прав базы. | Прямое указание владельца. Отмечено расхождение: в незакоммиченном `scripts/check-declarative-schema.mjs` (работа по QA-007) тем же номером помечена находка о расхождении декларативной схемы. Реестр этого документа — источник истины, поэтому находке по декларативной схеме следует присвоить QA-014; передано владельцу QA-007. | Принято |
| 2026-08-29 | Для QA-008 используется experimental `globalNotFound` установленного Next.js 16. | В локальной документации этой patch-версии стабильного эквивалента для нескольких root layouts нет; официальный флаг обрабатывает unmatched URL до рендера layout. Production build и root/public/admin E2E подтверждают HTTP 404 и полный документ. | Принято |

## Open questions

1. Каковы фактические production min/max/percentiles для product/category/brand image scale? Без read-only preflight bounds не утверждаются.
2. Какая SVG sanitizer/library корректно работает в текущем server runtime и сохраняет разрешённую vector semantics?
3. Какие resolution/CRF/codec/GOP дают минимальное измеренное время до первого движущегося кадра для startup-tier без заметной деградации, и какие параметры синхронизации/crossfade обеспечивают незаметный переход к утверждённой high-quality версии на mobile/desktop?
4. Какой TTL draft/staging, лимит file size/count и retention отвечают реальному admin workflow?
5. Какой безопасный механизм запуска cleanup/reconciliation доступен в целевой инфраструктуре?
6. Какие exact full-set/subset contracts нужны каждой reorder-сущности, особенно filtered/page product reorder?
7. Когда и в каком порядке миграция QA-013 применяется к production? Локально она проверена полностью, но на production у `anon`/`authenticated` сейчас реально есть TRUNCATE на всех таблицах каталога, и до применения миграции этот доступ сохраняется. Нужно решение владельца по окну применения вместе с остальными неприменёнными миграциями фаз 3–4.

## Ограничения среды baseline

- Browser/E2E coverage ещё не создано и не должно считаться существующим.
- Доступность Docker/local Supabase, browser binaries и remote schema в фазе 0 не подтверждалась.
- Production data distributions, remote Storage policies и runtime metrics не измерялись.
- Локальные unit/build результаты предыдущих проверок не заменяют повторную проверку после каждой фазы.
- Remote infrastructure не менялась и не проверялась в фазе 0.

## Порядок релиза

### Обязательные pending migrations

Ни одна из перечисленных миграций к production не применялась. Все они входят
в будущий согласованный выпуск как обязательные, в порядке номеров.

| Миграция | Фаза | Что несёт | Почему обязательна |
|---|---:|---|---|
| `20260826150000_storage_upload_limits` | 3 | `file_size_limit` и `allowed_mime_types` трёх bucket каталога | Часть закрытого QA-012 |
| `20260826170000_product_image_staging` | 3 | private bucket, таблица и 4 функции staged upload | Часть закрытого QA-004; без неё новая форма товара не работает |
| `20260827090000_admin_auth_reservation` | 4 | таблица броней и 3 функции; бронь до PBKDF2 | Часть закрытого QA-005; приложение уже вызывает новые RPC |
| `20260827120000_least_privilege_client_roles` | 4A | минимальные права клиентских ролей и default privileges | Часть закрытого QA-013; **до применения на production у `anon` сохраняется TRUNCATE на всех таблицах каталога** |

Перед публикацией отдельно проверяются: полный список pending migrations и его
совпадение с этой таблицей, порядок применения, совместимость выкатываемого
приложения с обеими схемами, и состояние `origin/main`. До этой проверки
применение не начинается.

### Шаги

1. Завершить локальные фазы и полный release candidate verification.
2. Получить отдельное разрешение на remote changes и подтвердить backup/rollback owner.
3. Provision private staging bucket, policies и lifecycle в совместимом режиме; старый поток пока сохранить.
4. Применить backward-compatible DB migrations/RPC/grants, проверить migration history, schema, RLS и advisors.
5. Развернуть приложение, совместимое и с новой, и с переходной схемой.
6. Выполнить production smoke: public catalog/search/product/404, admin login, обратимая product mutation, upload/finalize, hero/SEO/a11y; проверить logs/metrics.
7. Наблюдать agreed window и выполнить orphan/rate-limit reconciliation checks.
8. Только отдельным поздним релизом убрать старый upload/RPC contract после доказанного отсутствия обращений и отдельного approval.

## Status changelog

| Дата | Фаза | Изменение | Проверки | Результат |
|---|---:|---|---|---|
| 2026-08-24 | 0 | Documentation contract и independent verification завершены; подготовлены реестр QA-001…QA-012, traceability, зависимости, DoD, полномочия и release order; добавлена ссылка из README. | Tracked `git diff --check`; отдельная whitespace-проверка untracked документа; структура/ID/link target | Фаза 0 завершена после независимой проверки; локальный checkpoint будет создан отдельно; дефекты фаз 1–8 остаются открытыми |
| 2026-08-24 | 1 | Реализован fail-closed local runtime wrapper и Playwright harness: public/admin smoke, изолированные auth/catalog fixtures, responsive/a11y/browser-observer evidence, QA-002…QA-012 regression probes и CI artifacts без auth state. | `npm run check` PASS (69 files/290 tests); scoped ESLint/TypeScript PASS; Playwright list 43, smoke list 12; preflight штатно остановлен из-за недоступного Docker daemon | Implemented, verification pending: QA-001 и остальные QA ID остаются открытыми до полного запуска на поддерживаемом Docker + Chromium окружении |
| 2026-08-25 | 1 | Real smoke прошёл 12/12; prior full result superseded после обнаруженной race между streamed home/loading overlay и responsive assertions. Evidence также зафиксировал P2 density каталога и overlay WhatsApp на 320/390; design fix отложен до фазы 7. | `npm run e2e:smoke` PASS 12/12; полный baseline требует финального rerun после readiness/observer corrections | Implemented, verification pending: фазу 1 не закрывать до успешного полного rerun |
| 2026-08-25 | 1 | Финальный независимый локальный rerun завершён; superseded history выше сохранена. Зафиксированы P2 evidence для density каталога, overlay WhatsApp и большого белого scroll interval; review отложен до фазы 7. | Preflight PASS; production build PASS; smoke 12/12 PASS; full suite exit 0. Всего 43 outcome: 33 ordinary PASS, 5 expected `test.fail` (QA-006/007/008/009/011), 5 `fixme` (QA-002/003/004/005/012), 0 unexpected. Observer, axe, responsive matrix, fixture/auth cleanup, artifact isolation и освобождение 3101 PASS; пользовательский 3001 не затронут. CI не запускался. | Фаза 1 завершена, QA-001 закрыт. Windows Node 24 `UV_HANDLE_CLOSING` и `NO_COLOR`/`FORCE_COLOR` stderr остаются non-blocking P2 при корректном exit/cleanup; QA-002…QA-012 открыты |
| 2026-08-26 | 2 | QA-012 (bounds): read-only измерение production scale-значений; миграция `20260825090000_visual_scale_bounds` с нормализацией и CHECK на трёх колонках; общий `normalizeVisualScale` для UI и сервера; `min`/`max` в трёх инпутах. QA-003: миграция `20260825120000_strict_reorder_contracts` — семь RPC на модели слотов, общий контракт набора, обязательный parent scope для подкатегорий и фотографий, нормализация существующего порядка. QA-002: миграция `20260826090000_atomic_product_mutations` — `create_product_with_relations` / `update_product_with_relations` одной транзакцией, CAS по `expected_updated_at` с `FOR UPDATE`, разрешение slug и порядка внутри транзакции; удалены клиентские `resolveSubcategoryId` и `validateProductReferences`; добавлен `product-rpc-error` для доставки намеренных сообщений админу. Побочно: исправлено расхождение `prod.sql` по `categories.type`; устранена гонка в `assertCriticalControlsInsideViewport` (`count()` не ждал стриминга); тест scope-аргументов сделан устойчивым к CRLF; `.tmp-*/` внесены в оба списка игнорирования. | Полный прогон на восстановленном Docker: `supabase db reset --local` PASS (18 миграций); pgTAP **129/7 файлов PASS** (фаза 2 добавила 53); grants новых RPC проверены отдельно — EXECUTE только у `service_role`, у `anon` и `authenticated` доступа нет; `npm run check` PASS (316 тестов / 73 файла); `git diff --cached --check` PASS; `npm run build` PASS; e2e admin **14/14 PASS**; e2e public 21 PASS / 2 FAIL (внешний блокер, см. итог). Регресс-доказательства: снятие `target_product_slug`, снятие поля версии из формы, подстановка 100/−1/0 и удаление CRLF-нормализации роняют соответствующие проверки, возврат — чинит. Сквозная проверка CAS выполнена через живой supabase-js: подтверждена микросекундная точность версии и форма ошибки конфликта. | QA-002 и QA-003 закрыты; QA-012 закрыт наполовину (bounds), content validation переходит в фазу 3. **Незакрытые внешние ограничения, НЕ являющиеся PASS и не относящиеся к фазе 2:** (1) e2e public — `публичная карточка товара доступна по стабильному route` и `responsive baseline / catalog @ 375x812` падают на `net::ERR_ABORTED`; воспроизведено при отложенных изменениях фазы 2, следовательно принадлежит QA-007 — передано владельцу QA-007, не чинилось; (2) `npm run schema:declarative` FAIL: расхождение целиком по `admin_credentials` и `admin_auth_events_*` (миграция `20260821171707`), объявления этих объектов исключены из checkpoint как чужие — закрывается владельцем QA-007; ни один объект фазы 2 в расхождение не входит. До устранения не запускать `supabase db diff`. Remote миграции не применялись; production изменялся только чтением. |
| 2026-08-26 | 3 | QA-012 (вторая половина, закрывает ID). Вход в фазу выявил дефект серьёзнее заявленного: `uploadBrandLogo` и `uploadCategoryImage` не выполняли **никакой** серверной проверки, расширение бралось из `file.name`, а файл попадал в публичный bucket как есть; логотипы при этом по умолчанию считались SVG. Сделано: общий `validateRasterImage` выделен из `validateProductImage`; добавлен `upload-validation.ts` — тип и расширение выводятся из проверенного содержимого, а не из имени файла (закрыта подмена вида «вредоносный файл с именем photo.jpg»); SVG-логотипы обезвреживаются `isomorphic-dompurify` (профиль svg + явные FORBID_TAGS/FORBID_ATTR) с **отказом**, а не тихой вычисткой; для категорий SVG не принимается (в каталоге его нет); `insertProductImage` также переведён на расширение из содержимого; миграция `20260826150000_storage_upload_limits` задала `file_size_limit` 8 МБ и `allowed_mime_types` трём bucket каталога. Значения по измерению production: крупнейший файл 299 КБ, логотипы ~3 КБ, SVG всего 2. | `npm run check` PASS (330 тестов / 74 файла, +14); pgTAP **138/8 файлов PASS** (+9); `supabase db reset --local` PASS; `npx tsc --noEmit` PASS. Регресс-доказательство: отключение отказа в `sanitizeSvg` роняет 5 из 6 сценариев атак, шестой ловит независимая проверка результата (`residualDanger`) — она добавлена намеренно, чтобы решение не зависело только от внутреннего списка удалённого в библиотеке. Ложное срабатывание на чистом файле (служебный BODY от HTML-обёртки DOMPurify) найдено тестом и исправлено фильтром артефактов разбора. | QA-012 закрыт. QA-004 (staged direct upload, draft/finalize, cleanup по расписанию) остаётся открытым — продолжение фазы 3. Внешние блокеры без изменений: e2e public и расхождение декларативной схемы по `admin_credentials` принадлежат QA-007. Remote миграции не применялись; production изменялся только чтением. |
| 2026-08-26 | 3 | QA-004 (закрывает ID). Фотографии больше не передаются внутри общего запроса Server Action: браузер получает короткоживущую подписанную ссылку (5 мин) и грузит каждый файл отдельно в приватный bucket `product-image-staging`, а сервер скачивает объект, повторно проверяет содержимое, применяет выбранный режим обработки и только затем переносит в публичный bucket. Лимит количества проверяет БД (`claim_product_image_staging`) — единственное место, где видна вся сессия. Порядок прикрепления задаёт явный список идентификаторов из формы, поэтому отменённый снимок не прикрепляется. Повтор безопасен: публичный путь выводится из id учётной записи, запись защищена проверкой по URL. Уборка — служебный адрес приложения по расписанию Vercel (SQL удалять файлы не может: `storage.protect_delete`), fail-closed без `CRON_SECRET`, порядок «сначала файл, потом учётная запись». Старый путь удалён полностью: `formData.getAll("photos")` и `name="photos"` — 0 вхождений. | e2e admin **15/15 PASS**, включая новый сквозной сценарий (выбор файла → независимая загрузка → сохранение → фото прикреплено); `npm run check` 341/75 PASS; pgTAP 151/9 PASS; live-проверки: подписанная ссылка принимает обычный PUT, промежуточный bucket закрыт от публики и от скачивания без подписи, токен привязан к своему пути (подмена отклонена), bucket отвергает посторонний MIME, уборка идемпотентна. Регресс-доказательство: обнуление списка идентификаторов в форме роняет сквозной тест (35 с ожидания вместо 4 с), возврат — чинит. | QA-004 закрыт, фаза 3 завершена. Попутно найдены и исправлены два реальных дефекта конфигурации Next: `images.remotePatterns` жёстко требовал `https` (локальный стек работает по http), и Next 16 блокирует оптимизацию картинок с приватных IP — включён `dangerouslyAllowLocalIP`, вычисляемый из hostname Supabase, поэтому в production послабление физически невозможно. Внешние блокеры без изменений: e2e public и расхождение декларативной схемы по `admin_credentials` принадлежат QA-007. Remote миграции не применялись. |
| 2026-08-27 | 5 | QA-006: выполнен только read-only замер baseline, реализация не начиналась. Зафиксированы характеристики обеих 2K-версий, normal/throttled измерения времени до первого движущегося кадра, подтверждение неподвижного кадра на медленной сети, отсутствие master-файла и признак открытия заставки по `loadeddata` вместо подтверждённого воспроизведения. Подробности — в подразделе «Измеренное состояние baseline» фазы 5. Утверждённые требования и DoD фазы 5 не изменялись. | ffprobe по обоим derivative; HEAD к production Storage; браузерные замеры Playwright с эмуляцией сети (без ограничения и ~1,5 Мбит/с) на desktop 1440×900 и mobile 390×844; отдельное 60-секундное наблюдение throttled mobile. Изменений не вносилось: код, видео, Supabase, заголовки и deploy не затрагивались; к production выполнялись только чтения. | QA-006 остаётся `Открыт`. Реализация заблокирована до предоставления исходного master и последующего визуального утверждения перехода; повторное сжатие существующего derivative запрещено решением пользователя. Снятие `Cache-Control: no-cache` отложено тем же решением. Работа продолжается фазой 4 (QA-005). |
| 2026-08-27 | 4 | QA-005 (закрывает ID). Проверка пароля выполнялась ДО проверки лимита: `login` считал PBKDF2, и только потом вызывал `registerLoginAttempt`, поэтому заблокированный перебирающий всё равно расходовал дорогой хеш на каждом запросе — защита работала усилителем нагрузки. Тот же порядок был в `changeAdminPassword`. Миграция `20260827090000_admin_auth_reservation`: таблица `admin_auth_reservations`, общий `record_admin_auth_failure` (одно правило блокировки на оба пути), `begin_admin_auth_attempt` (бронь ДО PBKDF2 под advisory-локом: проверка блокировки, бюджет одновременности, сверка брошенных броней) и идемпотентный `finish_admin_auth_attempt`. Приложение: `beginAuthAttempt`/`finishAuthAttempt` в `login-rate-limit.ts`, оба пути перестроены. Прежний `register_admin_login_attempt` намеренно сохранён — политика отката требует убирать старый контракт отдельной поздней миграцией. TTL брони 30 с, бюджет одновременности 3. | pgTAP: новый `admin_auth_reservation.test.sql` **17/17 PASS** (валидация входа, выдача брони, бюджет одновременности, единичный учёт, идемпотентность повтора, блокировка после 5 неудач, отказ в броне заблокированному ключу без создания записи, засчитывание брошенной брони). `npm run check` **350 тестов / 77 файлов PASS**; `npm run build` PASS; e2e admin **15/15 PASS**. Регресс-доказательство: возврат прежнего порядка (хеш раньше брони) роняет 2 из 8 проверок `auth-reservation-order.test.ts`, восстановление — чинит. Права новых функций проверены по `pg_proc.proacl`: только `postgres` и `service_role`; при первой попытке обнаружено, что Supabase выдаёт EXECUTE новым функциям для `anon`/`authenticated` через default privileges и `REVOKE ... FROM PUBLIC` их не снимает — исправлено явным `FROM PUBLIC, anon, authenticated` по образцу `harden_admin_security`. | QA-005 закрыт, фаза 4 завершена. Попутно устранён собственный недочёт фазы 3: объекты `product_image_staging` (таблица, 5 констрейнтов, 4 функции, 2 индекса) не были объявлены в `supabase/schemas/prod.sql` — добавлены. **Новая находка, НЕ относящаяся к QA-005 и не исправлявшаяся:** три pgTAP-проверки прав `anon` (`anon cannot delete products`, обе hotspot-RPC) падают; воспроизведено при полностью отложенных изменениях фазы 4, то есть предшествует ей. Проверено вживую: `anon` действительно имеет табличные GRANT на `products` и EXECUTE на `restore_vehicle_hotspots`, но фактическое удаление даёт `DELETE 0` — RLS держит, данные не уязвимы; это ослабление защиты в глубину, а не открытая дверь. Требует отдельного разбора и, вероятно, собственного QA ID. Расхождение декларативной схемы теперь ровно по `admin_credentials*` (QA-007). Remote миграции не применялись; push и deploy не выполнялись. |
| 2026-08-27 | 4A | QA-013 (закрывает ID). Внеочередная фаза безопасности по находке, обнаруженной в фазе 4. **Аудит:** сняты фактические права `anon`, `authenticated` и `service_role` на все таблицы, последовательности и функции `public`; отдельно read-only проверено production. На production у обеих клиентских ролей на всех 11 таблицах каталога есть `SELECT, REFERENCES, TRIGGER, TRUNCATE, MAINTAIN`, и тот же набор автоматически получает каждая новая таблица через default privileges схемы; функции на production закрыты корректно (клиентским ролям доступен только `search_catalog_products`, `normalize_catalog_search` — через PUBLIC). Локально набор шире: полный `ALL` на 13 таблицах, EXECUTE на 29 функциях из 34, права на `admin_audit_log_id_seq`. Источник в обоих случаях один — default privileges, а не наши миграции: доказано созданием пробной таблицы и функции ролью `postgres`. Достижимость через настоящий Data API с `anon`-ключом проверена отдельно: запись отбивалась RLS, TRUNCATE через PostgREST не выражается, `anon` — `NOLOGIN`, динамического SQL, представлений и таблиц без RLS в схеме нет — поэтому S1, а не S0. **Исправление:** миграция `20260827120000_least_privilege_client_roles` — схемные и глобальная записи default privileges, схемный `REVOKE ALL` по всем таблицам, последовательностям и функциям, затем `SELECT` на 11 таблицах каталога и `EXECUTE` на двух функциях поиска для `anon`/`authenticated`, полный доступ подтверждён `service_role`. `normalize_catalog_search` получила явный GRANT: она вызывается внутри `search_catalog_products` с правами вызывающего и раньше держалась на снятом умолчании PUBLIC. Декларативная схема приведена к тому же состоянию: 22 каталожные строки переведены на `GRANT SELECT`, старые `REVOKE ... FROM PUBLIC` дополнены `"anon", "authenticated"`, добавлены REVOKE для `admin_audit_log`, `admin_login_rate_limits` и `admin_audit_log_id_seq`, переписан блок default privileges. `supabase db diff` намеренно не запускался — расхождение по `admin_credentials` (QA-007) сделало бы его опасным, миграция написана вручную и сверена с фактическим состоянием базы. | `supabase db reset --local` PASS (23 миграции); pgTAP **201/11 файлов PASS** (+33 новых в `client_role_privileges.test.sql`). Попутно этим же исправлением закрыты три проверки, падавшие с фазы 4 (`anon cannot delete products` и обе hotspot-RPC) — они и были симптомом QA-013. Сверка объявленного и фактического: списки GRANT для клиентских ролей в `prod.sql` и в живой базе совпадают строка в строку. Через анон-ключ локального Data API: чтение всех 11 таблиц и вложенных выборок 200, поиск 200, а `DELETE`, `INSERT`, `import_products_batch`, `reorder_products`, `create_product_with_relations` и чтение `admin_audit_log` — 42501. `npm run check` PASS (352 теста / 78 файлов); `npm run build` PASS с пререндером каталожных страниц (то есть публичное чтение под анон-ключом работает); e2e admin **15/15 PASS**; e2e public **25 PASS / 3 skipped / 0 FAIL**. Регресс-доказательства (три независимых): возврат `TRUNCATE`+`INSERT` роли `anon` роняет 5 проверок, включая живые; возврат глобального умолчания EXECUTE для PUBLIC роняет 2 проверки default privileges; возврат схемного `GRANT ALL ON TABLES` для `anon` роняет проверку новой таблицы. Живые проверки сверяют текст ошибки, а не только SQLSTATE: у отказа по GRANT и у нарушения RLS один и тот же 42501, и без сверки текста тест продолжал бы проходить после возврата лишнего GRANT. | QA-013 закрыт локально. **Не закрыто и требует решения владельца:** миграция к production не применялась, поэтому на production TRUNCATE у клиентских ролей сохраняется — внесено в open questions. Побочно: два pgTAP-теста получили явный `grant execute` на свои `pg_temp`-помощники — прямое следствие снятия умолчания PUBLIC. Расхождение по номеру: в незакоммиченном `scripts/check-declarative-schema.mjs` (QA-007) номер QA-013 присвоен находке о расхождении декларативной схемы; по реестру этого документа ей следует стать QA-014 — передано владельцу QA-007. Чужие файлы (`package.json`, `scripts/check-declarative-schema.mjs`) в коммит не включались. Push и deploy не выполнялись; production изменялся только чтением. |
| 2026-08-29 | 4B | QA-014 (закрывает ID). Расхождение декларативной схемы: восстановлена таблица `admin_credentials` с тремя констрейнтами и исправлено имя CHECK у `admin_auth_events` — в файле значилось `admin_auth_events_key_hash_check`, в базе он называется `admin_auth_events_attempt_key_hash_check`. Имя не было опечаткой в базе: в миграции CHECK объявлен безымянно на колонке, и имя присваивает Postgres, поэтому в тексте миграции его нет и текстовая сверка ошибку не ловит. Проверка `scripts/check-declarative-schema.mjs` доведена до рабочего состояния: ограничение времени на снятие схемы (120 с) вместо молчаливого ожидания Docker, разбор причины отказа на четыре понятных случая (Docker не отвечает, стек не запущен, CLI недоступен, таймаут), порог правдоподобия дампа и файла, чтобы неинициализированная база не объявлялась расхождением по всему файлу. Номер в комментариях и сообщениях исправлен на QA-014: QA-013 — другая находка, про права клиентских ролей. | `supabase db reset --local` PASS; pgTAP **201/11 файлов PASS**; `npm run schema:check` PASS (17 таблиц, 5 buckets); `npm run schema:declarative` PASS (155 объектов); `npm run lint` PASS. Регресс-доказательства проверки: удаление таблицы `admin_credentials` из файла роняет её с перечислением трёх объектов, переименование `admin_credentials_pkey` — с обеих сторон расхождения, восстановление — чинит; ветка таймаута проверена временным порогом и даёт понятное сообщение и код 1. | QA-014 закрыт. Новая миграция не создавалась, `supabase db diff` не запускался. Remote Supabase и Storage не изменялись; push и deploy не выполнялись. Согласование `main` с `origin/main` вынесено в отдельную задачу. |
| 2026-08-29 | 6 | QA-007 (закрывает ID). Runtime уже был исправлен ранее и не переписывался: `PRODUCT_SELECT` читает `meta_title`/`meta_description`, общий `buildProductMetadata` отдаёт их в title, description и Open Graph с цепочкой запасных значений, три рендерящих маршрута зовут его с собственным canonical, а `/product/[slug]` перенаправляет на единственный настоящий адрес карточки. Добавлены доказательства: fixture `tests/e2e/support/seo-products.ts` с уникальными sentinel-значениями строит четыре случая (заполненные SEO, пустые SEO, прямой маршрут, черновик) и новый обязательный набор `tests/e2e/public/product-metadata.spec.ts` из восьми проверок. Прежняя проба с `test.fail` удалена: она проходила именно тогда, когда дефект наблюдался, и после исправления не отличала рабочую реализацию от сломанной. | Все четыре публичных маршрута проверены по фактически отданному HTML. Заполненные поля: title `<meta_title> — AYPROM`, description = `meta_description`, og:title/og:description совпадают, canonical и og:url равны собственному адресу, `robots` отсутствует. Пустые поля: title = имя товара, description собирается из имени и артикула, длина ≤160. Черновик: RLS не отдаёт его ни по одному из трёх адресов, ни один из четырёх sentinel в HTML не найден; на успешных страницах его тоже нет. Секреты: в HTML карточки нет `service_role`, `SUPABASE_SECRET_KEY`, `ADMIN_SESSION_SECRET`, `ADMIN_PASSWORD`, `password_hash`, `session_version`, `admin_credentials` и заголовка JWT. Целевой набор — три последовательных прогона без retries, 8/8 в каждом, `net::ERR_ABORTED` ноль. `npm run check` 418 тестов PASS; `npm run build` PASS; unit metadata 14 PASS; `git diff --check` чисто; полный публичный набор 30 PASS при одном падении соседней пробы (см. ограничения). | QA-007 закрыт. **Ограничение 1:** чужой маршрут карточки отвечает 200, а не 404 — `generateMetadata` выполняется раньше защиты в теле и успевает начать поток, поэтому статус уже не изменить, а в голову попадают title и description настоящего товара. Дубля в индексе это не создаёт: Next проставляет туда `noindex`, что и закреплено тестом. Рекомендуемый основной адрес — тот, что возвращает `getProductHref` и на который ведёт `/product/[slug]`. Менять стратегию canonical без отдельного решения не стал. **Ограничение 2:** соседняя проба `публичная карточка товара доступна по стабильному route` в `public-smoke.spec.ts` продолжает плавать. Причина найдена и та же: `/product/[slug]` перенаправляет НА КЛИЕНТЕ уже после ответа 200, тест не дожидается назначения и оставляет незакрытую навигацию, которая попадает в журнал как `net::ERR_ABORTED`. В наборе QA-007 это решено ожиданием отрисованного H1; саму соседнюю пробу не трогал — она вне QA-007. **Ограничение 3:** предупреждение драйвера `GPU stall due to ReadPixels` от программной отрисовки в headless-Chromium может уронить любой тест, рендерящий карточку. Production не читался и не изменялся; push, deploy и миграции не выполнялись. |
| 2026-08-29 | 6 | QA-008 (закрывает ID). Включён официальный experimental `globalNotFound` Next 16 и добавлен автономный `app/global-not-found.tsx`: полный `<html lang="ru">`/`<body>` документ, собственные локальные Rubik и Razer F5 без сетевой зависимости, русские title/description, один H1 и ссылки на каталог и главную. Страница не зависит от public/admin root layouts и не загружает их data/chrome. Сегментная `src/app/(site)/not-found.tsx` сохранена. Ожидаемое падение admin-пробы удалено; добавлены обязательные root/public/admin E2E, HTTP contract, DOM, metadata, links, browser observer, a11y scan и screenshot evidence. | Baseline подтвердил ожидаемый дефект admin global-404. Первый исправленный прогон доказал HTTP 404 и правильный документ на всех трёх URL, но выявил два `meta[name=robots]`: один добавлял Next автоматически, второй был объявлен вручную. Ручной дубль удалён, тест закрепляет ровно один `noindex`. Финально: QA-008 **3/3 PASS** (runner 5/5 с auth setup/cleanup), production build PASS, browser observer чистый, serious/critical a11y violations — 0, визуальный screenshot проверен; `npm run check` **418/79 PASS**, `git diff --check`, lint, TypeScript и color contract PASS. | QA-008 закрыт, фаза 6 завершена. Существующий soft-404 совпавшего динамического товарного маршрута (`200 + noindex`) не относится к global unmatched routes и не изменялся. Chrome DevTools MCP в сессии отсутствовал; browser evidence получено настроенным изолированным Playwright harness. Push, deploy, Supabase, Storage и миграции не выполнялись. |
| 2026-08-29 | 7A | QA-009 (закрывает ID). Все disclosure в public Header связаны с управляемыми областями через стабильные React ids и `aria-controls`; labels mobile-controls меняются между «Открыть/Закрыть» и «Показать/Скрыть». Desktop Escape закрывает dropdown и возвращает фокус trigger. На mobile обработка разделена по уровню: первый Escape закрывает открытый подраздел и возвращает фокус его кнопке, второй закрывает главное меню и возвращает фокус opener. На всех затронутых keyboard controls добавлен единый видимый focus ring. Login outer wrapper заменён на `<main>`. `BackButton` проверяет Navigation API, с conservative same-origin fallback для браузеров без него: безопасная внутренняя history использует `router.back()`, прямой/внешний вход — `router.replace()` на `/catalog` либо `/` для корня каталога. Browser observer теперь отбрасывает только точный Chromium driver diagnostic `GPU stall due to ReadPixels`, не скрывая application WebGL errors. Добавлены 3 unit и 5 обязательных browser-regressions; прежний `test.fail` login удалён. | Baseline browser test подтвердил expected-fail из-за отсутствующего `<main>`. После исправления целевой production E2E QA-009 **5/5 PASS**: desktop/mobile keyboard-only, оба уровня Escape/focus return, `aria-controls`, accessible names/states, safe/native BackButton, semantic main; axe serious/critical — 0, screenshot mobile disclosure проверен визуально. `npm run check` **421 тест / 80 файлов PASS**; production build, lint, TypeScript, color contract и `git diff --check` PASS. Полный public запуск дал 30 PASS / 4 FAIL / 3 skipped / 4 not run: один новый RSC abort был корректно классифицирован и после этого целевой набор повторно прошёл 5/5; оставшиеся падения вызваны уже открытым QA-011 — под параллельной нагрузкой Next/Chromium иногда сообщает о неиспользованных preloaded font/CSS/icon, причём падают и прежние home smoke/responsive. | QA-009 закрыт локально, фаза 7A завершена. Полный release candidate пока не зелёный из-за QA-011; QA-006 закрывается production smoke, QA-010 и QA-011 открыты. Chrome DevTools MCP в сессии отсутствовал, поэтому DOM/roles, keyboard, axe, console/network observer и screenshot проверены изолированным Playwright. Push, deploy, Supabase, Storage и миграции не выполнялись. |
| 2026-08-31 | 8 | QA-011 (закрывает ID). Geist переведён с `next/font/google` на пять локальных variable-subset `woff2` (кириллица, latin, latin-ext, vietnamese) через `@font-face` в `globals.css`; сохранены прежние `unicode-range`, вес 100–900 и `font-display: swap`, поэтому визуальная типографика и объём загрузки не изменились. Источник, SHA-256 и OFL-лицензия зафиксированы в `src/app/fonts/geist/README.md`. `@next/env` закреплён прямой зависимостью вместо транзитивной. Отдельно найден и устранён дефект глобального preload: `global-not-found.tsx` объявлял `razer-f5` и `rubik` без `preload: false`, поэтому Next добавлял их `.p.`-копии в head **каждой** страницы — главная тянула три неиспользуемых шрифта, а сборка содержала по два экземпляра каждого файла. Объявления выровнены с `(site)/layout.tsx`. `poweredByHeader: false` подтверждён на живом сайте ранее. | Чистый `npm ci` PASS. `npm run check` **425 тестов / 81 файл PASS** (lint, TypeScript, color contract). Production build PASS без сетевых обращений; собранный `index.html` больше не содержит ни одного `rel="preload" as="font"`, а `.next/static/media` — ни одного дублирующего `.p.`-файла. Новый контрактный тест `tests/local-geist-font-contract.test.ts` проверяет пять файлов, их SHA-256, отсутствие `next/font/google` и Google Fonts URL во всём runtime/build-коде и запрещает `localFont` без `preload: false`; guard проверен на отрицательном сценарии — при откате правки он падает. E2E по контракту проекта (`CI=1`, 1 worker, 1 retry): **52 passed, 0 failed, 3 skipped, 1 flaky**, exit code 0. | QA-011 закрыт локально, фаза 8 завершена. Локальный checkpoint `38acacb`. Остаются открытыми QA-006 (закрывается production smoke) и QA-010 (mobile reflow). Отмечен отдельный хвост: при многопоточном локальном запуске (`workers` по умолчанию) публичные спеки товара нестабильны из-за гонок фикстур `qa-e2e-*`; под контрактным однопоточным прогоном воспроизводится как flaky и на продукт не влияет. Push, deploy, Supabase, Storage и миграции не выполнялись. |
